
// reconnectTries: 10, reconnectInterval: 5000ms, bufferMaxEntries: 64

// separate read pool with secondaryPreferred for analytics queries

// separate read pool with secondaryPreferred for analytics queries
